export default class Ship {
  length = 0;

  sunk = false;

  constructor() {
  }

  hit(num) {
    // Mark position as hit
  }

  isSunk() {
    // Return if all positions are hit
  }
}
